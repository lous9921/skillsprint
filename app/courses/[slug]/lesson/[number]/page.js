import Link from "next/link";
export default async function Lesson({params}){const {slug,number}=await params;return <><header className="nav"><div className="brand">Skill<span>Sprint</span></div><Link href={`/courses/${slug}`}>← Course</Link></header><main className="wrap"><span className="tag">Lesson {number} of 8</span><h1>Getting started</h1><div className="card section"><p>Learn one useful concept, then apply it immediately.</p><h3>Practice task</h3><p>Apply today's concept to a real situation and write down your result.</p><button className="primary">Mark lesson complete</button></div></main></> }